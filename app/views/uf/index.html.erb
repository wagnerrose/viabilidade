<p id="notice"><%= notice %></p>

<h1>Uf</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Sigla</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @uf.each do |uf| %>
      <tr>
        <td><%= uf.nome %></td>
        <td><%= uf.sigla %></td>
        <td><%= link_to 'Mostrar', uf %></td>
        <td><%= link_to 'Editar', edit_uf_path(uf) %></td>
        <td><%= link_to 'Apagar', uf, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova UF', new_uf_path %>
