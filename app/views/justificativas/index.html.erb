<p id="notice"><%= notice %></p>

<h1>Justificativas</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Código Justificativa</th>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @justificativas.each do |justificativa| %>
      <tr>
        <td><%= justificativa.nome %></td>
        <td><%= justificativa.cod_justificativa %></td>
        <td><%= justificativa.tipo %></td>
        <td><%= link_to 'Mostrar', justificativa %></td>
        <td><%= link_to 'Editar', edit_justificativa_path(justificativa) %></td>
        <td><%= link_to 'Apagar', justificativa, method: :delete, data: { confirm: 'Voce tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar nova Justificativa', new_justificativa_path %>
