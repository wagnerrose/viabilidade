<p id="notice"><%= notice %></p>

<h1>Justificativas</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Cod justificativa</th>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @justificativas.each do |justificativa| %>
      <tr>
        <td><%= justificativa.nome %></td>
        <td><%= justificativa.cod_justificativa %></td>
        <td><%= justificativa.tipo %></td>
        <td><%= link_to 'Show', justificativa %></td>
        <td><%= link_to 'Edit', edit_justificativa_path(justificativa) %></td>
        <td><%= link_to 'Destroy', justificativa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Justificativa', new_justificativa_path %>
