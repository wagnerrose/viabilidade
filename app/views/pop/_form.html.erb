<%= form_for(pop) do |f| %>
  <% if pop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pop.errors.count, "error") %> prohibited this pop from being saved:</h2>

      <ul>
      <% pop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :designacao %>
    <%= f.text_field :designacao %>
  </div>

  <div class="field">
    <%= f.label :detentor %>
    <%= f.text_field :detentor %>
  </div>

  <div class="field">
    <%= f.label :uf %>
    <%= f.text_field :uf %>
  </div>

  <div class="field">
    <%= f.label :cod_localidade %>
    <%= f.text_field :cod_localidade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
